//! Protocol verification — walks every documented EVO 8 control on real hardware.
//!
//! Stub — implemented in Step 8.

use anyhow::Result;

pub fn probe() -> Result<()> {
    anyhow::bail!("not implemented — see Step 8");
}
