//! Install / uninstall the kernel module, udev rules, and WirePlumber config.
//!
//! Stubs — implemented in Step 7.

use anyhow::Result;

pub fn install_driver() -> Result<()> {
    anyhow::bail!("not implemented — see Step 7");
}

pub fn uninstall_driver() -> Result<()> {
    anyhow::bail!("not implemented — see Step 7");
}
